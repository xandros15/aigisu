<nav class="navbar">
    <header class="navbar-header">
        <h1><a class="navbar-brand active" href="{{ path_for('web.home') }}">Aigisu</a></h1>
        <button type="button" class="navbar-toggle"
                onclick="document.querySelector('.navbar-collapse').classList.toggle('collapsed')"
                aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <i class="fa fa-bars" aria-hidden="true"></i>
        </button>
    </header>
    <div class="navbar-collapse">
        <ul class="nav navbar-nav">
            <li><a href="{{ path_for('web.units') }}">Units</a></li>
            {% if ident.is_guest %}
                <li><a href="{{ path_for('web.site.signin') }}">Sign In</a></li>
                <li><a href="{{ path_for('web.site.signup') }}">Sign Up</a></li>
            {% else %}
                <li><a href="{{ path_for('web.site.signout') }}">Logout</a></li>
            {% endif %}
            {% if ident.user.isOwner() %}
                <li><a href="{{ path_for('web.admin.user.index') }}">Users</a></li>
            {% endif %}
        </ul>
    </div>
</nav>
