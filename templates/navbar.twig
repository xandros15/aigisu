<nav class="navbar navbar-default navbar-static-top">
    <div class="container">
        <header class="navbar-header">
            <h1><a class="navbar-brand active" href="{{ path_for('web.home') }}">Aigisu</a></h1>
            <button type="button" class="navbar-toggle collapsed" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
            </button>
        </header>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li><a href="{{ path_for('web.units') }}">Units</a></li>
                {% if ident.is_guest %}
                    <li><a href="{{ path_for('web.site.signin.view') }}">Sign In</a></li>
                    <li><a href="{{ path_for('web.site.signup.view') }}">Sign Up</a></li>
                {% else %}
                    <li><a href="{{ path_for('web.site.signout') }}">Logout</a></li>
                {% endif %}
                {% if ident.user.isOwner() %}
                    <li><a href="{{ path_for('web.admin.user.index') }}">Users</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
